# 업데이트 내역

## v1.1 (2025-01-12)

### 주요 기능 추가

#### 1. 주차 범위 확장
- 주차 범위를 1-5주차에서 1-8주차로 확장
- 6주차, 7주차, 8주차 지원 추가
- 주차별 오프셋 설정 추가 (6주차: 180, 7주차: 216, 8주차: 252)

#### 2. 수동 주차 선택 기능
- 본사 파일 로드 후 수동으로 주차를 선택할 수 있는 기능 추가
- 콤보박스를 통해 월과 주차를 수동으로 설정 가능
- 수동 선택한 주차가 파일명 생성 및 데이터 매핑에 반영됨

#### 3. 비밀번호 관리 개선
- 프로그램 시작 시 `config.json`의 `default_password` 값으로 비밀번호 자동 입력
- 본사 파일 로드 성공 시 입력한 비밀번호를 `config.json`에 자동 저장
- 비밀번호 없이 파일 열기 시도 후 실패 시에만 비밀번호 입력 요청

#### 4. 수수료 및 익일정산 기능 추가
- 수수료 계산 기능 추가
  - 계산 방식: "건당 금액" 또는 "정산금액 대비 비율"
  - 건당 금액: 본사 파일 "종합" 시트 F열 값 × 수수료 값 (음수로 입력)
  - 정산금액 대비 비율: 정산금액 × (수수료 값 / 100) (음수로 입력)
- 익일정산 기능 추가
  - 정산금액 대비 비율 방식만 지원
  - 정산금액 × (익일정산 값 / 100) (음수로 입력)
- 테이블뷰에 수수료 계산방식, 수수료, 익일정산 입력 UI 추가
- 라이더별 수수료 및 익일정산 값 저장 기능

#### 5. 주민번호 입력 기능 추가
- `calculation_method="resident"` 추가
- 본사 파일 "주민번호" 시트에서 이름 기준으로 주민번호 조회
- 지점 파일 "주간정산" 시트 D열(이름) 기준으로 주민번호 자동 입력

#### 6. add_to_result 계산 방식 추가
- `calculation_method="add_to_result"` 추가
- 본사 파일의 여러 범위 값을 열방향 합산
- 지점 파일의 기존 수식에 합산 값을 빼기로 적용
- 예: `=IF(ISBLANK(D6),"",SUM(J6:L6))` → `=IF(ISBLANK(D6),"",SUM(J6:L6))-5000`

### UI 개선

#### 1. 테이블뷰 개선
- 수수료 테이블뷰 열 구성 변경
  - "성함" 열 너비 절반으로 축소
  - "수수료 계산방식" 콤보박스 추가 (선택, 건당 금액, 정산금액 대비 비율)
  - "수수료" 체크박스 → 텍스트 입력으로 변경
  - "익일정산" 열 추가
- 테이블뷰 열 너비 고정 및 레이아웃 유지 기능
- 수수료 계산방식 변경 시 동기화 로직 개선 (기본값으로 변경 시 다른 셀 미변경)

#### 2. 메인 창 레이아웃 조정
- 왼쪽/오른쪽 패널 비율 조정 (1:1)
- 테이블뷰 열 제목이 모두 보이도록 창 크기 및 레이아웃 조정
- 스크롤바 제거 및 고정 열 너비 설정

### 기능 개선

#### 1. 데이터 정렬 기능
- 주간정산 시트 데이터를 이름 기준 오름차순 정렬
- 월간정산 시트 데이터를 이름 기준 오름차순 정렬
- 정렬 시 수식의 행 번호 자동 업데이트
- 병합 셀 처리 개선
- "합계" 행은 정렬에서 제외

#### 2. 수수료 계산 로직 개선
- 건당 금액 방식: 수식 대신 계산된 숫자 값 입력
- 정렬 시 수식 업데이트 로직 개선
- 수식 감지 로직 보강

#### 3. 파일 저장 개선
- 지점 파일 저장 시 "주민번호" 시트 보존
- 본사 파일의 "주민번호" 시트를 지점 파일로 복사하여 보존

### 버그 수정

1. 수동 주차 선택 시 데이터 매핑 위치 오류 수정
2. 팀미션비 달성 인원 적용 오류 수정 (동적 "성함" 매핑 찾기)
3. 테이블뷰 열 너비 변경 문제 수정
4. 정렬 시 MergedCell 오류 수정
5. 수수료 계산방식 기본값 변경 시 다른 셀 변경 문제 수정

### 기술적 개선

- 동적 매핑 찾기 로직 개선 (하드코딩된 열 참조 제거)
- 수식 업데이트 로직 개선 (정렬 시 행 번호 자동 조정)
- 병합 셀 처리 로직 개선
- 에러 처리 및 로깅 개선

---

## v1.0 (2024-11)

초기 릴리스
- 기본 매핑 기능
- 1-5주차 지원
- 6가지 계산 방식 (simple_copy, conditional_copy, simple_sum, conditional_sum, date_calculation, unique_extraction)
- 월간정산 생성
- 비밀번호 보호 파일 지원
