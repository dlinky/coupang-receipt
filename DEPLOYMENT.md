# 배포 가이드

## Windows 실행 파일 빌드

### 사전 요구사항

1. Windows 10 이상
2. Python 3.11 이상 설치
3. 인터넷 연결 (의존성 다운로드용)

### 빌드 방법

#### 방법 1: 배치 스크립트 사용 (권장)

1. 프로젝트 루트 디렉토리에서 `build.bat` 실행:
```cmd
build.bat
```

2. 빌드가 완료되면 `dist` 폴더에 `쿠팡 정산서 프로그램(v1.0).exe` 파일이 생성됩니다.

#### 방법 2: 수동 빌드

1. 가상 환경 생성 및 활성화:
```cmd
python -m venv .venv
.venv\Scripts\activate
```

2. 의존성 설치:
```cmd
pip install -r requirements.txt
```

3. PyInstaller로 빌드:
```cmd
pyinstaller build_exe.spec
```

### 배포 패키지 구성

배포 시 다음 파일들을 함께 배포해야 합니다:

```
배포폴더/
├── 쿠팡 정산서 프로그램(v1.0).exe
└── config/
    ├── config.json
    └── mapping.json
```

### 사용자 배포

1. 배포 폴더를 사용자에게 전달
2. 사용자는 `쿠팡 정산서 프로그램(v1.0).exe`를 더블클릭하여 실행
3. Python 설치가 필요 없음 (모든 의존성이 포함됨)

### 문제 해결

#### 빌드 실패 시

1. Python 버전 확인 (3.11 이상 필요)
2. 모든 의존성이 설치되었는지 확인:
```cmd
pip list
```

3. PyInstaller 버전 확인:
```cmd
pyinstaller --version
```

#### 실행 파일이 작동하지 않을 때

1. Windows Defender나 백신 프로그램이 차단하는지 확인
2. 관리자 권한으로 실행 시도
3. 콘솔 창을 표시하여 에러 메시지 확인:
   - `build_exe.spec`에서 `console=True`로 변경 후 재빌드

#### 파일 크기가 큰 경우

- PyInstaller는 모든 의존성을 포함하므로 파일 크기가 큽니다 (약 100-200MB)
- 이는 정상적인 현상입니다

### 빌드 옵션 커스터마이징

`build_exe.spec` 파일을 수정하여 빌드 옵션을 변경할 수 있습니다:

- **아이콘 추가**: `icon='path/to/icon.ico'` 추가
- **콘솔 창 표시**: `console=True`로 변경
- **파일 이름 변경**: `name='새로운이름'` 수정

### 업데이트 배포

프로그램을 업데이트할 때:

1. 코드 변경 후 빌드 스크립트 실행
2. 새로운 `쿠팡 정산서 프로그램(v1.0).exe` 파일 생성
3. 사용자에게 새 실행 파일과 `config` 폴더 업데이트 안내

### 보안 고려사항

- 실행 파일은 코드 서명되지 않았으므로 Windows에서 경고가 표시될 수 있습니다
- 내부 사용 목적이므로 문제없지만, 필요시 코드 서명 인증서를 사용할 수 있습니다

